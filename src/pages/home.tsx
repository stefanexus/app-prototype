import HomeView from '../sections/home/home-view';

// Home dashboard tab. The AppLayout already wraps this in MobileShell +
// bottom navigation, so we render content only.
export default function HomePage() {
  return <HomeView />;
}
